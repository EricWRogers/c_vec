#ifdef _WIN32
#include <windows.h>
#endif

#include <stdio.h>

#include "vec.h"

int main(int argc, char* argv[]) {
    // array example
    int wholeNumbers[5];

    wholeNumbers[0] = 0;

    printf("%d\n", wholeNumbers[0]);

    // vec
    double* numbers = NULL;

    return 0;
}
